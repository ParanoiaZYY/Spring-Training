<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="site.paranoia.mapper.UserMapper">

    <select id="selectUserList" resultType="site.paranoia.domain.User">
        select * from s_user;
    </select>
</mapper>