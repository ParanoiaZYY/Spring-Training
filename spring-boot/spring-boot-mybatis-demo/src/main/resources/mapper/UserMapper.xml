<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="site.paranoia.mapper.UserMapper">

    <select id="selectById" resultType="site.paranoia.domain.User">
        select name,age,email from user where id = #{id}
    </select>

    <select id="selectAllUser" resultType="site.paranoia.domain.User">
        select name,age from user;
    </select>
</mapper>