<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="site.paranoia.mapper.OrderMapper">

    <select id="selectById" resultType="site.paranoia.domain.Order">
        select name,age,email from order where id = #{id}
    </select>

    <select id="selectAllUser" resultType="site.paranoia.domain.Order">
        select name,age from order;
    </select>
</mapper>